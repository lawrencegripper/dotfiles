#!/bin/bash

set -e

# Take new snapshot
zsysctl save -s

# Get latest dotfiles
chezmoi update

# Do updating
flatpak update -y
sudo apt update
sudo apt upgrade -y
brew update
brew upgrade
sudo snap refresh