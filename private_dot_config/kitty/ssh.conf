share_connections yes

# Todo: Make a light weight dotfiles for SSH session
# hostname *
# remote_kitty if-needed
# copy .aliases .zshrc .fzf.zsh .fzf .profile .config/atuin/config.ssh # This takes too long and doesn't really work
# Ideally we'd hook up atuin too...

